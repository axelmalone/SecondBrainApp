{
  "name": "second-brain-app",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native, local-first second brain — M1 vault I/O layer",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && cp src/renderer/index.html dist/renderer/index.html && mkdir -p dist/renderer/fonts && cp src/renderer/fonts/*.woff2 dist/renderer/fonts/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval:grounding": "tsx eval/grounding/run.ts",
    "eval:proposals": "tsx eval/proposals/run.ts",
    "dev": "npm run build && electron .",
    "start": "electron ."
  },
  "dependencies": {
    "@automerge/automerge": "^2.2.8",
    "@xenova/transformers": "^2.17.2",
    "markdown-it": "^14.2.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^22.10.0",
    "electron": "^33.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
